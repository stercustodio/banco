# from banco.serializers import SaldoSerializer
# from banco.validators.valida_saldo import (data_credito, data_debito,
#                                            saldo_final, valor_credito)
# from rest_framework import serializers

# serializer = SaldoSerializer()

# SaldoSerializer():
#     data_credito = DateField(read_only=True, validators=[data_credito])
#     valor_credito = FloatField(read_only=True, validators=[valor_credito])
#     data_debito = DateField(read_only=True, validators=[data_debito])
#     valor_debito = FloatField(read_only=True, validators=[valor_debito])
#     saldo_final = FloatField(read_only=True, validators=[saldo_final])
